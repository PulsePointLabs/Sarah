// Shared by the browser player and the Node renderer. Keep this in one module so
// a completed render cannot become invisible because the two sides drift apart.
export const REVIEW_VIDEO_RENDER_VERSION = "session_review_video_v20_no_production_slates";
